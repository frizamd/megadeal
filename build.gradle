subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    dependencies {

        compile 'org.springframework:spring-context:4.1.4.RELEASE'
        testCompile "junit:junit:4+"

    }
}